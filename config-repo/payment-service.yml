server:
  port: 8006

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_database

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASS:guest}

razorpay:
  api:
    key: ${RAZORPAY_API_KEY:}
    secret: ${RAZORPAY_API_SECRET:}

stripe:
  api:
    key: ${STRIPE_API_KEY:}
